# React Frontend Setup

This folder is the React frontend for the e-commerce application.

## Requirements

- Node.js 18+ recommended
- npm 9+ recommended

## Run project

```bash
cd client
npm install
npm start
```

App runs on `http://localhost:3000`.
